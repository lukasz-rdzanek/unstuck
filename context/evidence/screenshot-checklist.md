# 10xDevs 3.0 — Screenshot Checklist (Builder + Architect + Champion)

> Capture list for the certification forms. Each row: a stable **name**, the **link** to open,
> and **what the frame must show**. Public URLs need no login; only B4 requires an authenticated user.
> Suggested storage: `context/evidence/builder or architect or champion`.

## 🚀 10xBuilder (form #1 — Builder folder)

## 🏆 10xChampion — Project #1: AI code-review pipeline (form #2 - Champion folder)

## 🏆 10xChampion — Project #2: shared AI artifact registry (form #2 - Champion folder)

## 🔧 10xArchitect (form #2)

No screenshots required. The deliverable is the two-pager [context/evidence/architect/architect-report.md](./architect/architect-report.md), submitted as text.

---

**Notes**

- The repository is **private**: either add reviewers as collaborators (`@przeprogramowani`, `@psmyrdek`, `@mkczarkowski`; Builder also `przeprogramowani@gmail.com`) so the GitHub links open for them, or make each screenshot self-sufficient (URL, job, and version visible in-frame).
- B5: screenshot the green run on `master` (linked above), not the PR run — the last PR CI run on `chore/archive-ci-cd-review` is red while `master` after merge is green.
- C5/C6: if the `/pkgs/npm/...` link does not open directly, the package is also under profile → Packages (`github.com/lukasz-rdzanek?tab=packages`) or the repo's right-hand "Packages" section.
